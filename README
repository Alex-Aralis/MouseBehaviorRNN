This project uses the Keras Python library to implement an RNN that learns from datasets produced by the multicap script in MouseInputCapture.

In order to run this program the Keras module must be installed in the Python environment used for interpretation.


